[project]
name = "kubeflow-trainer-examples"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material",
    "mkdocs-nav-weight",
    "jupyter",
    "notebook",
    "pre-commit",
    "ruff",
    "kubeflow",
]
