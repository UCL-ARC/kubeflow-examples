[project]
name = "kubeflow-trainer-examples"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material",
    "mkdocs-nav-weight"
]

[dependency-groups]
dev = [
    "pre-commit",
]
